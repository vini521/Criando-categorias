@model IEnumerable<ExemploEF.Models.Cliente>

@{
    ViewData["Title"] = "Lista de Clientes";
}

<div class="container mt-4">
    <div class="text-center">
        <h1 class="fw-bold">Lista de Clientes</h1>
        <p class="text-muted">Gerencie os clientes cadastrados no sistema.</p>
    </div>

    <div class="d-flex justify-content-end mb-3">
        <a asp-action="Create" class="btn btn-success rounded-3 fw-bold">
            <i class="bi bi-plus-circle"></i> Criar Novo Cliente
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-bordered align-middle">
            <thead class="table-dark">
                <tr>
                    <th class="text-center">@Html.DisplayNameFor(model => model.Nome)</th>
                    <th class="text-center">@Html.DisplayNameFor(model => model.Nascimento)</th>
                    <th class="text-center">Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.Nome)</td>
                        <td class="text-center">@Html.DisplayFor(modelItem => item.Nascimento)</td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@item.ClienteID" class="btn btn-warning btn-sm rounded-3">
                                <i class="bi bi-pencil me-1"></i> Editar
                            </a>
                            <a asp-action="Details" asp-route-id="@item.ClienteID" class="btn btn-info btn-sm rounded-3 text-white">
                                <i class="bi bi-eye me-1"></i> Detalhes
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.ClienteID" class="btn btn-danger btn-sm rounded-3">
                                <i class="bi bi-trash me-1"></i> Excluir
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script>
        // Inicializa tooltips do Bootstrap
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
        var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl);
        });
    </script>
}